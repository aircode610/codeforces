#include <bits/stdc++.h>
#define int long long

using namespace std;

int32_t main() {
  int t; cin >> t;
  while (t--){
    int n, m;
    cin >> n >> m;
    int x[n+1];
    for (int i = 1; i <= n; i++)
      cin >> x[i];
    
  }
  return 0;
}
